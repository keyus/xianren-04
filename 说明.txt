src  目录为，webpack 工程文件


dist 目录为，已经编译完成，生成的前端文件.可直接提供后端使用

注意（使用服务器浏览资源）;原因，资源文件暂时采用决对路径


----------------


执行命令前准备

安装 node.js  新版完成后可以运行


依次热行命令：

npm install        #安装依赖

npm run build      #编译文件到dist目录

npm run start      #启动内置的服务器